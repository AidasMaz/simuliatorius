using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class gynimui : MonoBehaviour
{
    //public GameObject parent;
    //public Text t;
    //private bool touched = true;

    //private void OnCollisionEnter2D(Collision2D collision)
    //{
    //    if (touched)
    //    {
    //        t = GameObject.Find("-Gynimui-").GetComponent<Text>();

    //        touched = false;
    //        int eil = int.Parse(t.text);
    //        t.text = (eil + 1).ToString();
    //        Debug.Log("Paimtas");

            //Instantiate((Resources.Load("prefab") as GameObject), 
            //    new Vector3(transform.position.x + 2, transform.position.y, transform.position.z), 
            //    Quaternion.identity);

    //        Destroy(gameObject);
    //    }
    //}
}
